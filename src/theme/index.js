import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        primary: {
            light: "#EBACF0",
            main: "#FF0099",
            dark: "#C850D1",
        },
        secondary: {
            main: "#9554EC"
        }
    }   
});

export default theme;






